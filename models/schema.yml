
version: 2

models:
  - name: revenue
    description: Table containing the realised revenue
    columns:
      - name: order_no
        description: The unique indentifier of the column
        tests:
          - not_null
          - unique
      - name: revenue
        description: The revenue generated by the specific order
      - name: changed_at
        description: When the order was changed for the last time